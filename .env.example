# Optional: outbound notifications from CLI runs (see README).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: long-polling timeout (seconds) for `ai-dev-bot telegram` (max 50).
TELEGRAM_POLL_TIMEOUT=45

# Optional: directory containing *.json task files (default: ./samples/tasks).
# TASKS_DIR=./samples/tasks
