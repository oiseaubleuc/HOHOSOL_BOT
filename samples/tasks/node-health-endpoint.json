{
  "id": "NODE-12",
  "title": "Expose /health for load balancer",
  "description": "Add a lightweight JSON health endpoint that does not hit the database, suitable for k8s probes.",
  "projectRoot": "../fixtures/sample-node",
  "priority": "low",
  "labels": ["node", "ops"]
}
